<Window x:Class="TellerApp.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Teller" Height="420" Width="520"
        WindowStartupLocation="CenterScreen"
        Background="#F5F7FA">
    <Window.Resources>
        <SolidColorBrush x:Key="BrushPrimary" Color="#1F305E"/>
        <SolidColorBrush x:Key="BrushAccent" Color="#E34234"/>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Padding" Value="18 10"/>
            <Setter Property="Margin" Value="0 8 0 0"/>
            <Setter Property="Background" Value="{StaticResource BrushPrimary}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.45"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#29437F"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#162B55"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="AccentButton" BasedOn="{StaticResource PrimaryButton}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BrushAccent}"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F25548"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#C83226"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.45"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="TELLER PANEL"
                   FontSize="26"
                   FontWeight="Bold"
                   Foreground="#1F305E"/>

        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="16" Margin="0 18 0 10">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0 0 0 8">
                    <TextBlock Text="Loket:" VerticalAlignment="Center" FontSize="14" Foreground="#5F6B76" Margin="0 0 6 0"/>
                    <ComboBox x:Name="LoketCombo" Width="90" SelectionChanged="LoketCombo_SelectionChanged">
                        <ComboBoxItem Content="1" IsSelected="True"/>
                        <ComboBoxItem Content="2"/>
                        <ComboBoxItem Content="3"/>
                        <ComboBoxItem Content="4"/>
                        <ComboBoxItem Content="5"/>
                    </ComboBox>
                </StackPanel>
                <TextBlock Text="Nomor Terakhir Dipanggil:" FontSize="14" Foreground="#5F6B76"/>
                <TextBlock x:Name="CurrentNumberText"
                           Text="—"
                           FontSize="54"
                           FontWeight="Bold"
                           Foreground="#1F305E"
                           Margin="0 -4 0 0"/>
                <TextBlock x:Name="LoketLabel"
                           Text="Loket: -"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="#E34234"
                           Margin="0 -4 0 0"/>
                <TextBlock x:Name="StatusText"
                           Text="Status: Belum terhubung"
                           FontSize="14"
                           Foreground="#5F6B76"
                           Margin="0 10 0 0"/>
            </StackPanel>
        </Border>

    <Grid Grid.Row="2">
        <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button x:Name="ConnectButton"
            Grid.Row="0"
            Style="{StaticResource PrimaryButton}"
            Content="Hubungkan"
            Click="ConnectButton_Click"/>
        <Button x:Name="NextButton"
            Grid.Row="1"
            Style="{StaticResource AccentButton}"
            Content="Panggil Berikutnya"
            IsEnabled="False"
            Visibility="Visible"
            Click="NextButton_Click"/>
    </Grid>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="SettingsButton" Content="Pengaturan" Width="110" Margin="8,0,0,0" Click="SettingsButton_Click"/>
        </StackPanel>

        <TextBlock Grid.Row="3"
                   Text="ESC untuk keluar"
                   HorizontalAlignment="Right"
                   Foreground="#5F6B76"
                   FontSize="12"
                   Margin="0 20 0 0"/>
    </Grid>
</Window>